print('h')  #use small case letter for print
#Print('H')  #Capital Print cause error


name = "Najaf Ali"
Name = "Hussnain Imran"
NAME = "Ali Meesam"
#name, Name And NAME are three different variables. try to print these variables

#Use the correct lowercase spelling for python keywords/function. 
#Forexample if, else, for while. THERE Is no REPEAT UNTIL in python
input()
print()

#Python treats A and a as completely different characters. 
#Always use the exact same capitalization when referring to a variable, function, or class.
myList = [5, 67, 7, 88, 9]

print(myList) #no error because we use same capitalization.
#print(mylist) #Error because mylist ≠ myList

#There's no datatype declaration for basic datatypes. Python identfies datatype on it's own. 
#It is known as Dynamic Typing

age = 17
Student_Name = "Aliza"
marks = 85.5

#Check datatype of above variables with assigned values
age = 17
print(type(age))